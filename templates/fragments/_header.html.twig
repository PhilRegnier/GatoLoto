<header class="header">
    <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #D6BAEE;">
        <div class="container-fluid">
            <a class="navbar-brand logo" href="{{ path('main_accueil') }}">
                <img class="logo" src="{{ asset('resources/logo.png') }}" width="60" height="60" alt="Logo Gato Loto"/>
                <span class="logo">Gato Loto</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                    <a class="nav-link" aria-current="page" href="{{ path('main_accueil') }}">Accueil</a>
                    <a class="nav-link" href="{{ path('etudiant_liste') }}">Les étudiants</a>
                    <a class="nav-link" href="{{ path('etudiant_ajout') }}">Ajouter un étudiant</a>
                </div>
            </div>
        </div>
    </nav>
</header>
